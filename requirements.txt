numpy>=1.26
scipy>=1.11
psycopg[binary,pool]>=3.1
httpx>=0.27
fastapi>=0.110
uvicorn[standard]>=0.27
apscheduler>=3.10,<4
typer>=0.12
rich>=13.7
mcp>=1.2
PyYAML>=6.0
# Fernet, for the per-source access tokens that must be replayed to clone a
# private repository. Already present transitively; named here because this
# is now a direct dependency and a transitive one can vanish without notice.
cryptography>=42
